package io.iamkyu.specification;

public class MemberSpec {
    // public static Specification<Member> isAdult() {
    //     return (Specification<Member>) (root, query, builder) ->
    //             builder.greaterThan(root.get(Member_.age), 20);
    // }
}
